---
import { SITE } from '../config/site';
---

<footer class="border-t border-white/5 bg-base-100/80 backdrop-blur-xl">
	<div
		class="max-w-7xl mx-auto px-4 py-12 grid gap-8 lg:grid-cols-[1.5fr_1fr_1fr] items-start"
	>
		<div class="space-y-4">
			<p class="text-xs uppercase tracking-[0.3em] text-quartz/80">
				Fabien Brocklesby
			</p>
			<p class="text-base leading-relaxed text-slate-300/90 max-w-xl">
				Building human-centred creative software from Nelson Tasman. Light Lane
				is how we scale laser, CNC, and 3D printing workflows for teams who
				think in colours, not G-code.
			</p>
			<div class="flex gap-4 text-sm opacity-80">
				<a
					href={SITE.social.linkedin}
					target="_blank"
					rel="noreferrer"
					class="hover:opacity-100">LinkedIn</a
				>
				<a
					href={SITE.social.github}
					target="_blank"
					rel="noreferrer"
					class="hover:opacity-100">GitHub</a
				>
				<a
					href={SITE.social.instagram}
					target="_blank"
					rel="noreferrer"
					class="hover:opacity-100">Instagram</a
				>
			</div>
		</div>
		<div class="space-y-2">
			<h3
				class="font-semibold text-sm uppercase tracking-[0.2em] text-quartz/80"
			>
				Contact
			</h3>
			<a
				href={`mailto:${SITE.contact.email}`}
				class="text-slate-200 hover:text-white transition"
				>{SITE.contact.email}</a
			>
			<a
				href={`tel:${SITE.contact.phone.replace(/[^+\d]/g, '')}`}
				class="block text-slate-200/80 hover:text-white transition"
				>{SITE.contact.phone}</a
			>
			<p class="text-slate-400 text-sm">{SITE.contact.location}</p>
		</div>
		<div class="space-y-2">
			<h3
				class="font-semibold text-sm uppercase tracking-[0.2em] text-quartz/80"
			>
				Navigate
			</h3>
			<nav class="flex flex-col gap-2 text-sm text-slate-300/90">
				<a href="#about" class="hover:text-white transition">About</a>
				<a href="#timeline" class="hover:text-white transition">Timeline</a>
				<a href="#light-lane" class="hover:text-white transition">Light Lane</a>
				<a href="#services" class="hover:text-white transition">Services</a>
				<a href="#press" class="hover:text-white transition">Press</a>
				<a href="#contact" class="hover:text-white transition">Contact</a>
			</nav>
		</div>
	</div>
	<div class="border-t border-white/5 py-6">
		<div
			class="max-w-7xl mx-auto px-4 flex flex-col md:flex-row md:items-center justify-between gap-4 text-xs text-slate-500 uppercase tracking-[0.28em]"
		>
			<span
				>© {new Date().getFullYear()} {SITE.name}. All rights reserved.</span
			>
			<span>Designed & built with Astro, Tailwind 4, DaisyUI 5.</span>
		</div>
	</div>
</footer>
